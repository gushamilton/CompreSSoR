% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{read_sumstats_batch}
\alias{read_sumstats_batch}
\title{Read canonical variants from several compressed GWAS files}
\usage{
read_sumstats_batch(
  stores,
  variants,
  columns = c("chromosome", "base_pair_location", "effect_allele", "other_allele",
    "beta", "standard_error"),
  threads = 1L
)
}
\arguments{
\item{stores}{A non-empty list or character vector of Pcodec stores.}

\item{variants}{A canonical \code{chromosome:position:REF:ALT} vector shared by
every store, or one such vector per store in a list.}

\item{columns}{Output columns requested from every store.}

\item{threads}{Number of stores to decode concurrently inside the shared
Pcodec process.}
}
\value{
A list of decoded data frames in the same order as \code{stores}.
}
\description{
Starts the Pcodec runtime once for the complete batch. This is substantially
faster than repeated \code{\link[=read_sumstats]{read_sumstats()}} calls when an analysis extracts a
small instrument set from several GWAS files.
}
\examples{
\dontrun{
read_sumstats_batch(
  c(exposure = "exposure.cpr", outcome = "outcome.cpr"),
  c("1:100000:A:G", "1:200000:C:T"),
  columns = c("chromosome", "base_pair_location", "effect_allele",
              "other_allele", "beta", "standard_error")
)
}
}
